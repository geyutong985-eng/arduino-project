# 硬件接线速查表

只看每张表，把“模块接口”接到“ESP32 接口”即可。

## 1. 两个 IMU 模块

上臂 IMU 和前臂 IMU 大部分接法一样，只有 `AD0` 不一样。

| IMU 模块接口 | 上臂 IMU 接到 ESP32 | 前臂 IMU 接到 ESP32 |
|---|---|---|
| `VCC` | `3V3` | `3V3` |
| `GND` | `GND` | `GND` |
| `SCL` | `GPIO22` | `GPIO22` |
| `SDA` | `GPIO21` | `GPIO21` |
| `AD0` | `GND` | `3V3` |
| `INT` | 不接 | 不接 |
| `XDA` | 不接 | 不接 |
| `XCL` | 不接 | 不接 |

## 2. Flex 弯曲传感器

如果是三线模块：

| Flex 模块接口 | 接到 ESP32 |
|---|---|
| `VCC` / `+` | `3V3` |
| `GND` / `-` | `GND` |
| `AO` / `SIG` / `S` / `OUT` | `GPIO34` |
| `DO` | 不接 |

如果是裸 Flex 电阻片：

| 裸 Flex / 电阻接口 | 接到 ESP32 |
|---|---|
| Flex 一端 | `3V3` |
| Flex 另一端 | `GPIO34` |
| 10kΩ 电阻一端 | `GPIO34` |
| 10kΩ 电阻另一端 | `GND` |

## 3. Pressure 压力传感器

| Pressure 模块接口 | 接到 ESP32 |
|---|---|
| `VCC` / `+` | `3V3` |
| `GND` / `-` | `GND` |
| `AO` / `SIG` / `S` / `OUT` | `GPIO35` |
| `DO` | 不接 |

## 4. 手掌气泵驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO18` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 手掌气泵正极 |
| `OUT-` / `M-` | 手掌气泵负极 |

## 5. 手掌气阀驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO19` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 手掌气阀正极 |
| `OUT-` / `M-` | 手掌气阀负极 |

## 6. 小臂气泵驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO4` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 小臂气泵正极 |
| `OUT-` / `M-` | 小臂气泵负极 |

## 7. 小臂气阀驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO5` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 小臂气阀正极 |
| `OUT-` / `M-` | 小臂气阀负极 |

## 8. 震动马达 1 驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO2` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 震动马达 1 正极 |
| `OUT-` / `M-` | 震动马达 1 负极 |

## 9. 震动马达 2 驱动模块

| 驱动模块接口 | 接到哪里 |
|---|---|
| `IN` / `SIG` / `S` | ESP32 `GPIO15` |
| `VCC` | 驱动模块电源正极 |
| `GND` | ESP32 `GND` |
| `OUT+` / `M+` | 震动马达 2 正极 |
| `OUT-` / `M-` | 震动马达 2 负极 |

## 10. 必须共地

| 需要接在一起的接口 |
|---|
| ESP32 `GND` |
| 两个 IMU 的 `GND` |
| Flex 的 `GND` |
| Pressure 的 `GND` |
| 所有驱动模块的 `GND` |
| 外部电源负极 |
