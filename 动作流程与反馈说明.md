# 动作流程与反馈说明

这份文档说明当前活动流程：用户按顺序完成动作，系统用气动和震动给出反馈。

## 1. 活动目标

用户需要完成一组动作流程：

```text
Half Raised → Picking → Pressure Press
```

也就是：

```text
先完成半抬动作
再完成拾取动作
最后按下压力传感器
```

系统会根据动作识别结果推进流程。如果用户停太久、顺序明显出错，系统会用震动提醒或重置流程。

## 2. 正常流程

| 阶段 | 系统等待什么 | 用户应该做什么 | 正常进入下一步 |
|---|---|---|---|
| S0 | 等待开始 | 做出 `HALF_RAISED` | 检测到 `HALF_RAISED` |
| S1 | 等待 `PICKING` | 从半抬继续做到拾取动作 | 检测到 `PICKING` |
| S2 | 等待按压 | 按下压力传感器 | 检测到按压 |
| 完成 | 回到初始 | 可以开始下一轮 | - |

如果用户动作比较快，系统没有捕捉到 `HALF_RAISED`，但直接检测到了 `PICKING`，也会宽松接受，直接进入 S2 等待按压。

## 3. 单路震动提醒

单路震动用于提醒用户“下一步还没完成”。

| 当前阶段 | 超时条件 | 触发哪个震动 | 震动模式 | 停止条件 |
|---|---|---|---|---|
| S1 等待 `PICKING` | 3 秒内没有检测到 `PICKING` | `GPIO2` 震动马达 | 震 1 秒，停 2 秒，循环 | 检测到 `PICKING` |
| S2 等待按压 | 2 秒内没有按压 | `GPIO15` 震动马达 | 震 1 秒，停 2 秒，循环 | 检测到按压 |

注意：3 秒和 2 秒内不会立刻震动。只有等待超时后才开始震动。

## 4. 流程重置

重置表示：当前流程作废，请用户重新开始。

重置提示使用两个震动马达同时震动：

```text
两个马达同时震动 2 秒
停止 1 秒
两个马达同时震动 2 秒
停止
回到 S0
```

这和普通提醒不同：

| 类型 | 震动方式 | 含义 |
|---|---|---|
| 普通提醒 | 单个马达，震 1 秒停 2 秒循环 | 请继续做下一步 |
| 重置提示 | 两个马达同时震 2 秒，停 1 秒，再同时震 2 秒 | 流程已重置，请重新开始 |

## 5. 会触发重置的情况

| 情况 | 触发条件 | 处理 |
|---|---|---|
| 等待 `PICKING` 太久 | S1 持续 8 秒还没有检测到 `PICKING` | 双震动提示重置 |
| 等待按压太久 | S2 持续 8 秒还没有按压 | 双震动提示重置 |
| 按压阶段倒退 | S2 中持续检测到 `HALF_RAISED` 超过 3 秒 | 双震动提示重置 |
| 用户提前按压 | S0 或 S1 中检测到按压 | 双震动提示重置 |

如果提前按压触发重置，系统会等用户松开压力传感器后，才接受下一轮流程。这样可以避免用户一直按着时重复触发重置提示。

## 6. 不会单独触发重置的情况

`UNKNOWN` 不会单独触发重置。

原因是从 `HALF_RAISED` 到 `PICKING` 的过程中，动作识别可能短暂显示 `UNKNOWN`。这属于正常过渡，不应该立刻判定失败。

当前处理方式是：

```text
UNKNOWN = 当前等待的目标还没出现
```

真正触发重置的是总等待超时：

```text
S1 等 PICKING 超过 8 秒
S2 等按压超过 8 秒
```

## 7. 和气动反馈的关系

震动流程主要负责“动作流程提醒”。

气动反馈仍按原来的传感器和动作条件工作：

| 模块 | 触发方式 |
|---|---|
| 手掌气动 | 压力传感器按下充气，松开放气 |
| 小臂气动 | 检测到 `HALF_RAISED` 且 Flex 弯曲后充气 |

所以完整体验是：

```text
动作识别推进流程
震动提示下一步或重置
压力传感器控制手掌气动
Half Raised + Flex 控制小臂气动
```

## 8. 当前关键时间

| 参数 | 时间 |
|---|---|
| 等 `PICKING` 的首次提醒 | 3 秒 |
| 等按压的首次提醒 | 2 秒 |
| 单路提醒震动模式 | 震 1 秒，停 2 秒 |
| 等 `PICKING` 的重置超时 | 8 秒 |
| 等按压的重置超时 | 8 秒 |
| S2 中持续倒退到 `HALF_RAISED` 的重置时间 | 3 秒 |
| 重置提示 | 双震 2 秒，停 1 秒，双震 2 秒 |
